apiVersion: extensions/v1beta1
kind: Ingress
metadata:
  name: {{.Value.Ingress.Name}}
spec:
  tls:
  - secretName: {{.Value.Ingress.Secret}}
  backend:
    serviceName: nodeport-service
    servicePort: 80
